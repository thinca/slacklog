--- a/src/gui_w32.c
+++ b/src/gui_w32.c
@@ -6653,8 +6653,12 @@ gui_mch_flush(void)
 #   endif
 
 #if defined(FEAT_DIRECTX)
+    proftime_T tm;
+    profile_start(&tm);
     if (IS_ENABLE_DIRECTX())
 	DWriteContext_Flush(s_dwc);
+    profile_end(&tm);
+    Trace(profile_msg(&tm));
 #endif
 
     GdiFlush();
