// API SKELETONS

{{- range $n, $m := .Types.FrontendAPI.Fields }}
{{- if hasSuffix $n "Handler" }}
{{- $fn := lowerCamel (trimSuffix $n "Handler") }}
{{- $pn := trimSuffix $m.Type "Handler" }}

func (ri *RESTImpl) {{$fn}}(p {{$pn}}Params, _ interface{}) middleware.Responder {
	// TODO: implement RESTImpl.{{$fn}}()
	return myerr.Respond(myerr.NotImplemented().Error())
}
{{- end }}
{{- end }}