[core]
	pager = (delta 2>/dev/null) || (bat 2>/dev/null) || less

[interactive]
	diffFilter = (delta --color-only 2>/dev/null || cat)

[pager]
  branch   = false
  ls-files = true
  config   = false
	log  = (delta 2>/dev/null) || (bat --plain -l gitlog 2>/dev/null) || less
	show = (delta 2>/dev/null) || (bat --plain -l gitlog 2>/dev/null) || less

[diff]
	colorMoved = default

[delta]
	features = color-line-numbers decorations
	whitespace-error-style = 22 reverse
	inspect-raw-lines = false

[delta "color-line-numbers"]
	line-numbers = true
	line-numbers-minus-style = "#444444"
	line-numbers-zero-style  = "#444444"
	line-numbers-plus-style  = "#444444"
	line-numbers-left-format  = "{nm:>4}┊"
	line-numbers-right-format = "{np:>4}│"
	line-numbers-left-style  = blue
	line-numbers-right-style = blue

[delta "decorations"]
	commit-decoration-style = bold cyan box ul
	file-style = bold red ul
	file-decoration-style = none

