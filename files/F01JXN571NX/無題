let userProcesses = []

export const setUserProcesses = (processes) => {
  userProcesses = [...userProcesses, processes]
}

export const getProcessesDefinition = () => {
  return [
    ...[userProcesses],
    ...[
      {
        name: "open-file",
        processes: openFileProcesses,
      },
    ],
  ]
}
